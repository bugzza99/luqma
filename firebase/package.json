{
  "name": "luqma-firebase",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "cd .. && firebase emulators:exec --only firestore \"node --test firebase/test/*.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0"
  },
  "dependencies": {
    "firebase-admin": "^14.3.0"
  }
}
