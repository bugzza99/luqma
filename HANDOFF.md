# برومت لـ VS Code

انسخ كل اللي تحت السطر ده وابعتهولي في VS Code.

---

اقرأ `CLAUDE.md` الأول. احنا في نص شغل متفق عليه بالكامل، والقرارات كلها متاخدة —
**متسألنيش فيها تاني، نفّذها.**

## أول حاجة قبل أي كود

شغّل محاكي أندرويد وجرّب التلات تطبيقات بنفسك، بالحسابات دي:

```
admin@luqma.app      luqma2026     AdminApp
merchant@luqma.app   luqma2026     MerchantApp
customer@luqma.app   luqma2026     CustomerApp
luqma.serv@gmail.com Luqma120899   AdminApp (حساب المالك)
```

اكتشف المشاكل الحقيقية بنفسك وصلّحها. **التطبيقات دي اتبنت واتختبرت بس عمرها ما اتشغلت
على جهاز**، وآخر مرتين ده حصل طلع باجّين كانوا هيقفوا الإطلاق:

- مانيفست MerchantApp مكسور من وقت شيل فايربيز — التطبيق مكانش بيتبني أصلاً
- الصلاحيات كانت بتتقري من صف المستخدم بدل التوكن — **محدش كان يقدر يدخل AdminApp، أبداً**

فالتشغيل الحقيقي مش تحصيل حاصل هنا. ابدأ بيه.

## القرارات المتفق عليها — كلها مقفولة

**الترتيب**: الصور ← واجهة العميل ← شبكة الأدمن ← الإشعارات.
الصور الأولانية لأن الواجهة الجديدة معظمها صور، ولو الواجهة اتعملت قبلها هتطلع مربعات رمادية.

**الإشعارات**: FCM لوحده. **استخدم مشروع `luqma-edku` الموجود** — التلات تطبيقات متسجلين
فيه بأسماء الحزم الصح (`com.luqma.customer` / `.merchant` / `.admin`). متعملش مشروع جديد.
`firebase-tools` مسطّب وداخل بحساب `bugzza99@gmail.com`.

**الإرسال**: الأوردر بيكتب صف في `push_outbox` في **نفس معاملة الأوردر**، و`pg_cron` بيفضّيه
ويبعت. مينفعش نداء HTTP جوه الـtrigger — FCM لو وقع الأوردر نفسه ممكن يفشل، وده أسوأ بكتير
من إشعار متأخر.

**أول إشعار**: أوردر جديد للتاجر، لوحده. ده الوحيد اللي لو مشتغلش نموذج العمل بيقع.
القناة `orders_critical` وصوت `new_order.wav` **مبنيين ومستنيين رسالة**.

**التوكنات**: `users.fcm_tokens` لكل الأدوار (كل حساب ليه صف في `users` من
`ensure_user_profile`). يتسجل عند الدخول، يتمسح عند الخروج، **وردّ FCM بيقول أنهي توكن باظ
فيتشال من المصفوفة**. الجزء الأخير ده هو المهم — من غيره بعد شهور بتبعت لعشر توكنات ميتة
وواحد حي والفشل بيبان كأنه عطل.

**الصور**: الأدمن والتاجر والمطبخ البيتي كلهم يرفعوا، وكله بيعدّي على نفس بوابة المراجعة.
صورة الأدمن بتتوافق فوراً — **وده شغّال من الأصل**، لأن `admin_media` نوعها `for all`
والسياسات بتتجمع بـOR. فيه اختبار مثبّته دلوقتي.

**الحجم**: تصغير على الموبايل قبل الرفع، ١٦٠٠px وجودة ٨٥٪. الـbucket بيرفض فوق ٢ ميجا وده
احتياطي مش المقاس الشغال.

**شبكة الأدمن**: تاخد مكان الشريط السفلي بالكامل على الموبايل — ١١ اختيار في `NavigationBar`
وده مكوّن مصمم لـ٣ لـ٥. على الكمبيوتر `NavigationRail` يفضل زي ما هو.
والمربعات **بأرقام حيّة**: «الصور ٣ مستنية»، «الشكاوى ٢ مفتوحة». **استعلام واحد بيرجّع كل
الأرقام**، مش ١١ استعلام.

**واجهة العميل شبه طلبات**: كروت بصور + دواير أقسام بصور + كاروسيل بانرات + شريط عنوان ثابت.
**من غير** شريط فلترة — إدكو فيها ٣٠ مطعم.

**ودي إعادة تنسيق للأقسام الموجودة، مش نظام جديد.** `HomeSectionRegistry` وترتيب الصفحة من
الأدمن **ممنوع يتلمسوا**. و`categoryChips` القسم الموجود هو اللي يبقى الدواير — مفيش نوع
قسم جديد. و`adSlot` الموجود هو اللي يبقى الكاروسيل.

**الكارت**: صورة، تقييم، وقت تحضير، سعر توصيل. **من غير أقل طلب** — أربع أرقام على كارت
صغير ضوضاء، والشرط متطبّق في السلة وموجود في صفحة المطعم.

**الكارت من غير صورة**: أول حرف من الاسم على خلفية ملوّنة من الاسم. يوم الإطلاق **مفيش ولا
مطعم عنده صورة**، فدي الحالة الغالبة مش حالة حافة.

**الكاروسيل**: كل ٥ ثواني، بيقف عند اللمس، **وبيتلغي خالص لو الجهاز مفعّل تقليل الحركة**.

**البحث**: مطاعم **وأصناف**. اللي بيكتب بيدوّر على أكلة مش على محل. من غير وجبات البيتي —
بتخلص بالليل، ونتيجة بحث بتختفي بعد ساعتين أوحش من إنها متظهرش.

**وقت التحضير**: عمود واحد `prep_minutes`، يظهر «٣٠ دقيقة تقريباً». مش مدى — إدكو كلها ١٠
دقايق بالموتوسيكل، والمتغيّر الحقيقي هو المطبخ.

**الأقسام**: جدول `cuisines` + جدول ربط. **مش** `menu_categories` — دي تقسيمة المطعم لنفسه
وبتتكرر لكل مطعم. والأدمن بس هو اللي بيكتب فيها: تاجر بيحط نفسه في دايرة مش بتاعته ده أرخص
إعلان في المنتج، والإعلان حاجة التجار بيدفعوا فيها.

## اللي خلص واتثبت

**كوميت `9a00bb4`** — كل ده أخضر ومتأكد منه:

- bucket `media` عام بسقف ٢ ميجا + ٣ سياسات على `storage.objects`
- `cuisines` + `merchant_cuisines` + `merchants.prep_minutes` + نوعين صور جداد
- `sweep_orphan_media()` ومهمة `pg_cron` بتنضّف الصور اليتيمة بعد ٧ أيام
- `MediaRepository.upload()` — **الميثود اللي عمرها ما كانت موجودة.** بتكتب مرتين ولازم
  الاتنين يتفقوا: لو صف الداتابيز فشل البايتات بتتشال
- `ImageCompressor` — ١٦٠٠px/٨٥٪، Dart خالص فبيتختبر من غير جهاز
- `LuqmaImage` — الحرف البديل. اللون من **مجموع رموز الاسم مش من `hashCode`**، لأن Dart مش
  ضامنة ثبات الهاش بين التشغيلات وكان التحديث هيعيد تلوين المدينة كلها. وفيه اختبار بيتأكد
  إن كل لون ممكن يطلع بيعدّي ٣:١

`٧٩٩ Dart · ٥٦ schema · ١٠٩ stack · ١٣٧ live` — كلهم خضر وقت الكوميت.

## اللي مش متكوميت — راجعه قبل ما تكمّل

الشغل ده مكتوب وبيعدّي `flutter analyze` على `luqma_core`، **بس مجموعة الاختبارات الكاملة
معدّتش عليه**:

- `MediaPicker` — المنتقي المشترك. ٧/٧ اختبارات خضر
- `image_source.dart` + `pickImageProvider` — الـseam، بدون افتراضي
- `gallery.dart` في MerchantApp وAdminApp — الملف الوحيد اللي بيعرف `image_picker`.
  CustomerApp مابيرفعش حاجة فمابيشيلش الإضافة ولا أذوناتها
- محرر «حول لقمة» — **المنتقي بدل خانة الـuuid.** كانت بتطلب منك تكتب معرّف صورة موافَق
  عليها بإيدك، ومفيش حاجة في المنتج كانت تقدر تعمل واحدة
- `MediaKind` أخد `aboutPhoto` و`cuisine`، وطابور المراجعة أخد اسمين ليهم
- `NotAnImageFailure` في عيلة `Failure` — `Failure` مغلقة فمينفعش نوع خاص بره مكتبتها
- المنتقي في `MenuEditor` — الصنف بقى ليه صورة في نفس الورقة

**⚠️ حاجة واحدة مش متأكد منها**: `apps/admin_app/test/about_editor_test.dart` — الفورم بقى
أطول من الشاشة فزرار الحفظ مابقاش بيتبني في `ListView` كسول، وحطيت `scrollUntilVisible`.
**التعديل ده اتكتب ومااتأكدش إنه أخضر.** شغّله الأول.

## اللي فاضل

### ١ — الصور، اللي باقي منها

- `Merchant.prepMinutes` مش في موديل Dart (العمود موجود في الداتابيز)
- `Cuisine` موديل و`CuisineRepository` — **مش موجودين خالص**
- المنتقي لسه مش موصّل في: الوجبة اليومية، لوجو وغلاف المطعم، بانر الإعلان
- محرر الأقسام في AdminApp — مش موجود
- **رقم إصدار «حول لقمة» لسه متكتوب في الكود**: `kLuqmaVersion = '1.0.0'` وجنبه كومنت بيقول
  إن `package_info_plus` مش dependency — **وهي بقت dependency** وبتُستخدم في بوابة التحديث
  الإجباري. المتفق عليه كان «الإصدار اللي التطبيق عارفه عن نفسه»

### ٢ — واجهة العميل

- `MerchantCard` — مش موجود
- دواير الأقسام: `categoryChips` لسه بيعرض **أربع كلمات متكتوبة في الكود** ومش بتفلتر حاجة
- `adSlot` يبقى كاروسيل
- شريط العنوان الثابت
- **شاشة البحث — خانة البحث دلوقتي `readOnly: true` و`onTap: () {}`، مش بتعمل حاجة خالص.**
  و`docs/04` بيقول حرفياً إن مفيش تبويب أقسام لأن «البحث بيغطي الباقي»
- **`selectedCuisineProvider`** — الدايرة في قسم والمطاعم في قسم تاني، والسجل بيبنيهم منفصلين
  ومايعرفوش بعض. محتاجين حالة مشتركة. لو مكتشفتهاش دلوقتي هتتكتب كـhack جوه أحد القسمين

### ٣ — شبكة الأدمن

مفيش منها حاجة. `_destinations` في `router.dart` فيه ١١ عنصر.

### ٤ — الإشعارات

مفيش منها حاجة غير القناة والصوت. محتاجة `push_outbox` + trigger + pg_cron + Edge Function
+ `firebase_messaging` + `google-services.json` × ٣.

### حاجات إنت اللي تعملها، مش أنا

- **كونسول Firebase**: امسح Firestore `(default)` المهجورة، وحسابين Auth
  (`luqma.serv@gmail.com` و`bugzza99@gmail.com`)، وبصمة OAuth القديمة `75:86:90:59:…` من
  تطبيق العميل. حاولت والحارس منعني، ومحاولتش ألف حواليه. **نضافة مش حريقة** — القواعد
  القديمة كانت مكتوبة صح ومفيش داتا حقيقية
- Play Console + Play App Signing
- مشروع Sentry للـDSN
- **أسماء مناطق إدكو الحقيقية** من شاشة الأماكن في AdminApp — اللي في `data/edku.json`
  أماكن صحيحة الشكل بس مش معرفة محلية، واسم منطقة غلط بيبعت الدليفري لناحية تانية
- **بدّل توكن Supabase اللي بعتهولي في الشات**، وامسح أو غيّر باسوردات الحسابات التجريبية
  اللي في `tool/seed-demo.mjs` قبل أول تاجر حقيقي — الباسوردات دي في المستودع

## قواعد الشغل ده

- **TDD**: اكتب الاختبار، شوفه أحمر، بعدين صلّح. مفيش استثناء
- **`tool\run_tests.ps1` من PowerShell مش من Git Bash** — الأخيرة بتكسر `PROGRAMFILES`
- **`-j 1` على `test_live`** إجباري
- **الـfakes مش هي النظام.** أي حاجة معتمدة على سياسة محتاجة اختبار في `supabase/test/stack`
  بتوكن حقيقي، جنب اختبار الـwidget
- **متخليش هجرتين يشاركوا نفس بادئة النسخة.** `ls supabase/migrations | sed 's/_.*//' | sort
  | uniq -d` لازم يطلع فاضي
- **cron في الهجرات لازم يتحرس بـ`pg_available_extensions`** — PGlite مافيهاش pg_cron
- **متكتبش لون في شاشة.** والبرتقالي `#D67F2B` ساقط على الكريمي (٢.٥٨) وشارته بتاخد نص
  غامق مش أبيض
- **متطابقش «نجح» على مجموعة اختبارات من غير ما تبص على الجدول.** الـstack قال `exit 0`
  وجواه «الجدول مش موجود» — كان بيشتغل على داتابيز نصّها متهاجر

ابدأ بالمحاكي، وبعدين كمّل من «اللي فاضل» بالترتيب.
